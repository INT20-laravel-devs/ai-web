export enum Routes {
  Home = "/",
  Chat = "/chat",
  Profile = "/profile",
  SignUp = "/auth/sign-up",
  SignIn = "/auth/sign-in",
}

export const navigation = [{ label: "Home", href: Routes.Home }];

export const footerNavigation = [{ label: "Home", href: Routes.Home }];
